name: deploy-docker-manylinux_2_28_x86_64_cuda_12.3

on:
    # push:
    #     branches:
    #         - main
    workflow_dispatch:
    release:
        types: 
            - published

jobs:
  build_docker:
        name: build
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Login to GitHub Container Registry
              uses: docker/login-action@v3
              with:
                registry: ghcr.io
                username: ${{ github.actor }}
                password: ${{ secrets.GITHUB_TOKEN }}

            - name: Build and push manylinux_2_28_x86_64_cuda-12.3
              id: docker_build
              uses: docker/build-push-action@v5
              with:
                  push: true
                  file: ./docker/manylinux_2_28_x86_64_cuda_12.3/Dockerfile
                  tags: ghcr.io/makslevental/manylinux_2_28_x86_64_cuda_12.3:latest

            - name: Image digest
              run: echo ${{ steps.docker_build.outputs.digest }}

  test_docker:
        needs: [build_docker]
        name: test
        runs-on: ubuntu-latest
        steps:
            - name: Install Snap
              run: |
                sudo apt update
                sudo apt install snapd

            - name: Install Docker
              run: |
                sudo snap install docker
                sudo groupadd -f docker
                sudo usermod -aG docker $USER
                newgrp docker

            - name: Pull Docker Image
              run: docker pull ghcr.io/makslevental/manylinux_2_28_x86_64_cuda_12.3

            - name: Run Docker Image
              run: docker run -t ghcr.io/makslevental/manylinux_2_28_x86_64_cuda_12.3 nvcc --version
